// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/jekyll
{
  "name": "Jekyll Site",
  // 你可以使用一个包含 Ruby 的基础镜像，或者使用 Jekyll 模板
  "image": "mcr.microsoft.com/devcontainers/jekyll:latest", // 推荐使用 Jekyll 模板镜像
  // 或者使用一个 Dockerfile 来安装 Jekyll
  // "dockerFile": "Dockerfile",

  "forwardPorts": [4000], // Jekyll 默认运行在 4000 端口
  "postCreateCommand": "bundle install", // 在容器创建后安装 Jekyll 依赖

  "customizations": {
    "vscode": {
      "extensions": [
        "esbenp.prettier-vscode",
        "DavidAnson.vscode-markdownlint"
        // 其他你需要的Markdown相关扩展
      ]
    }
  }
}
